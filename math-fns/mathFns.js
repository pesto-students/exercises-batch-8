
function sqrt(number) {
  return Math.sqrt(number);
}

function round(number) {
  return Math.round(number);
}

function power(baseNumber, exponent) {
  return baseNumber ** exponent;
}

export {
  sqrt,
  round,
  power,
};
