
function applyOperator(...args) {
  // if (args.length === 0)
  //   throw new Error('operator is not defined');
  // let result = 0;

  // switch (args[0]) {
  //   case '+':
  //     for (let i = 1; i < args.length; ++i) {
  //       result += args[i];
  //     }
  // }

  return args;
}

export {
  applyOperator,
};
